package com.aggrid.jpa.adapter.request.filter.advanced;

public enum TextAdvancedFilterModelType {
    equals,
    notEqual,
    contains, 
    notContains, 
    startsWith,
    endsWith,
    blank,
    notBlank,
    ;
}
